{
  "name": "memegenscript",
  "version": "11.3.0",
  "private": true,
  "description": "The free and open source API to generate memes, rewritten in TypeScript for Cloudflare Workers.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build:templates": "tsx scripts/build-templates.ts",
    "fetch:descriptions": "tsx scripts/fetch-descriptions.ts",
    "predev": "npm run build:templates",
    "dev": "wrangler dev",
    "predeploy": "npm run build:templates",
    "deploy": "wrangler deploy",
    "pretest": "npm run build:templates",
    "test": "vitest run",
    "pretypecheck": "npm run build:templates",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types"
  },
  "dependencies": {
    "@jsquash/jpeg": "^1.6.0",
    "@jsquash/png": "^3.1.1",
    "@jsquash/webp": "^1.5.0",
    "@resvg/resvg-wasm": "^2.6.2",
    "emojilib": "^2.4.0",
    "gifenc": "^1.0.3",
    "gifuct-js": "^2.1.2",
    "opentype.js": "^1.3.4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@types/node": "^24.0.0",
    "@types/opentype.js": "^1.3.8",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^4.1.11",
    "wrangler": "^4.40.0",
    "yaml": "^2.8.0"
  }
}
